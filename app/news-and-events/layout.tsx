const title = '';
export const metadata = {
    title,
};

export default async function Layout({
    children,
    params: {
        lng
    }
}: {
    children: React.ReactNode;
    params: { lng: string };
}) {

    return (
        <>
        </>
    );
}